# 조립공장 작업 루트

### 사용자 고정 2원칙 (절대 위반 금지)

1. 실제 프로그램 검증: 완료 보고 전 반드시 사용자의 실제 Chrome 화면에서 대상 동작을 직접 실행하고 결과 화면/산출물까지 read-back한다. 자동 테스트·코드 확인·HTTP 응답만으로 성공을 보고하지 않는다.
2. 상태 보존: 작업 전 상태 A의 입력값·선택값·이미지·생성 결과·로그를 보존하고 요청한 변경 A+B만 적용한다. `새 작업` 또는 명시적 초기화가 아니면 기존 자료를 지우거나 덮어쓰지 않는다. 변경 전후 회귀 비교가 없으면 완료로 보고하지 않는다.

상세 게이트는 [docs/VERIFICATION_INVARIANTS.md](docs/VERIFICATION_INVARIANTS.md)에 있으며, 이 규칙의 실제 증거가 없으면 보고를 중단하고 검증을 먼저 수행한다.

이 저장소의 기준 작업 루트는 `C:\Users\kua\Documents\GitHub\kuasangse`입니다.

- 작업 시작 전에 `git rev-parse --show-toplevel` 결과가 위 경로인지 확인합니다.
- 결과가 다르면 코드, 테스트, `.omo`, 로그를 만들거나 수정하지 말고 작업을 중단합니다.
- `C:\Users\kua\product-detail-generator`와 `C:\Users\kua\Documents\kua-sachyo`는 이 제품의 작업 루트가 아닙니다.
- 기존 사용자 변경·미추적 파일과 `output/local-archive` 산출물은 삭제하거나 되돌리지 않습니다.
- 하위 에이전트는 필요한 경우에도 1~2개만 사용하고, 완료 즉시 종료 상태를 확인합니다.

수동 루트 검증:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\verify_workspace_root.ps1
```

## 필수 검증 흐름

이 저장소의 개발은 `변경별 집중 검증`과 `매일 전체 회귀`의 투트랙으로 진행합니다.

1. 코드 수정 전 현재 실패를 재현하고, 재현 로그나 실패 테스트를 남깁니다.
2. 원인과 직접 관련된 범위만 수정하며 기존 이미지 생성·저장·전송 경로를 함께 바꾸지 않습니다.
3. 수정 직후 변경 부위 집중 테스트와 인접 공정 테스트를 실행합니다.
4. UI/UX 변경은 실제 브라우저에서 직접 클릭하고 진행·완료·실패 상태와 화면 잘림을 확인하며 스크린샷을 남깁니다.
5. 발견된 재발 버그는 `docs/REGRESSION_TEST_MATRIX.md`와 자동 회귀 실행기에 새 정답으로 추가합니다.
6. 매일 1회 `npm run verify:daily`를 실행하고, 실패가 있으면 새 변경을 더 쌓기 전에 원인을 처리합니다.
7. 실행 명령, 원문 로그, 스크린샷 또는 실제 화면 확인 없이 성공으로 보고하지 않습니다.

## A 상태 보존 불변식 (절대 규칙)

모든 코드 수정·복원·새로고침·상태 동기화는 아래 계약을 반드시 지킵니다.

- 작업 시작 시점의 A 상태(제품·DB/Cafe24 선택·필수값·대표/사이즈/이미지컷·VM 후보와 상세수집 결과)를 기준 스냅샷으로 취급합니다.
- 명시적인 `새 작업` 또는 사용자의 개별 `선택 해제`가 아닌 한, A의 값·파일·후보·진행률을 삭제하거나 빈 값으로 낮추면 안 됩니다. 변경 결과는 A+B여야 합니다.
- 같은 작업 스코프의 복원/hydration/reconcile 경계는 incoming snapshot이 비어 있거나 더 적은 경우 현재 상태를 보존해야 하며, `factoryPreserveProgressForSameWork` 중앙 가드를 우회하는 직접 snapshot 교체를 추가하지 않습니다.
- 자동 회귀는 최소한 선택 키·필수값·각 이미지 수·VM 상세수집 결과의 전후 개수와 상태를 비교해 하나라도 감소하면 실패해야 합니다. 새 작업만 전체 초기화를 허용합니다.
- 실제 Chrome 검증에서도 새로고침 전후 화면 read-back으로 같은 항목을 대조한 뒤에만 완료를 보고합니다.

## 정상 앱 인계 강제 게이트

- 앱 코드 수정은 한 가지 원인 단위로만 진행하고, 다른 수정과 묶지 않습니다.
- 완료 보고 전 `npm run verify:daily`와 `npm run verify:handoff`를 순서대로 실행합니다.
- `verify:handoff`는 전체 회귀 0건 실패, 검증 뒤 runtime source 무변경, bundle 일치를 모두 만족한 보고서만 승인합니다.
- 인계 게이트가 실패하면 새 수정을 더 쌓거나 정상 앱 완료로 보고하지 않습니다. 실패한 후보는 수정 전 정상 상태를 대체할 수 없습니다.

외부 비용이나 부작용이 있는 Vertex 실제 생성, VM 실마켓 수집, Cafe24 실제 등록, OAuth 재로그인은 자동 회귀에 넣지 않고 `docs/REGRESSION_TEST_MATRIX.md`의 실환경 수동 게이트를 따릅니다.

## 사용자가 직접 권한을 부여한 브라우저 작업

- 사용자가 현재 작업에서 로그인·보안·외부 설정 변경 권한을 명시적으로 부여하면, routine 로그인과 설정 작업을 사용자에게 다시 시키지 말고 Chrome 브라우저를 직접 조작해 끝까지 수행합니다.
- Cafe24 OAuth, API 연결, Developers 설정, 승인 화면은 먼저 API Hub 상태를 확인한 뒤 Chrome에서 실제 화면을 열고, Redirect URI 등록·저장·승인·재연결 결과까지 직접 검증합니다.
- `로그인해줘`, `연결해줘`, `재설정해줘`, `다시해봐` 요청을 받았을 때는 먼저 브라우저에서 실행을 시도하고, CAPTCHA·OTP·사람 확인처럼 사용자의 물리적 입력이 필수인 단계에서만 중단해 보고합니다.
- 로그인 화면에서 브라우저에 저장된 아이디·비밀번호가 자동 입력되는 경우 raw 값을 읽거나 출력하지 말고, 로그인 버튼을 직접 눌러 진행합니다. 저장정보로도 막히는 추가 인증 단계에서만 사용자에게 도움을 요청합니다.
- 사용자가 명시적으로 권한을 준 범위를 넘어 비밀번호·토큰·API 키를 출력하거나 저장하지 않습니다. 로그인 상태와 권한은 상태값으로만 확인하고 raw credential은 기록하지 않습니다.
- OAuth 자동 갱신은 재연결 성공 후 실제 API Hub `refresh-token` 호출과 만료 상태 확인으로 검증하며, refresh 권한 만료나 제공자 설정 불일치는 연결 완료로 보고하지 않습니다.

## Chrome 확장 제어 안정성 강제 규칙

- 사용자 Chrome은 `Default` 프로필(표시 이름 `호야`)의 공식 Codex 확장 연결 하나만 사용합니다. 다른 프로필이나 Computer Use를 임의 fallback으로 섞지 않습니다.
- Chrome 명령은 반드시 한 번에 하나씩 직렬 실행합니다. `Promise.all`, 병렬 탭 제어, 여러 탭 동시 닫기처럼 하나의 Native Messaging 연결에 요청을 겹치지 않습니다.
- 한 작업에서는 에이전트가 만든 최신 앱 탭 하나만 제어합니다. 시간 초과나 커널 재시작 뒤의 기존 탭 핸들은 재사용하거나 개별 조작하지 않고 폐기 대상으로 표시합니다.
- 새 Chrome 탭은 `tabs.new()`로 만든 뒤 반드시 같은 탭에서 `tab.goto(url)`을 호출해 이동합니다. `tabs.new({url})`의 `about:blank` 상태나 그 뒤 selector 실패를 확장 통신 실패로 오판하지 않습니다.
- 로드된 조립공장 화면에서는 전체 `domSnapshot()`을 요청하지 않습니다. 필요한 selector의 `count`, 짧은 텍스트, 상태 필드만 제한적으로 읽습니다.
- 사용자 포인터를 움직이지 않아도 되는 로컬 앱 버튼은 고유 locator인지 확인한 뒤 `press("Enter")`로 활성화합니다. 실제 포인터 클릭은 포인터 hit-test 회귀 검증에서만 사용합니다.
- 브라우저 명령이 시간 초과되면 같은 핸들에 재시도를 쌓지 않습니다. 연결을 새로 확인하고 새 앱 탭 하나에서 마지막 안전 checkpoint부터 재개합니다.
- selector 실패는 먼저 `tabs.list`와 새 탭의 URL·짧은 로드 상태로 분류합니다. 이 연결 왕복이 통과하면 페이지 렌더/탭 수명 문제로 기록하고 Native Messaging·확장 미설치 문제로 보고하지 않습니다.
- 에이전트가 만든 오래된 probe 탭은 하나씩 닫지 않습니다. 검증 마지막에 정확히 한 번의 `tabs.finalize`로 최신 인계 탭만 남기고 정리합니다.
- 확장 안정성 판정은 설치 여부가 아니라 `tabs.list` → 짧은 상태 읽기 → 버튼 활성화 → 상태 read-back이 연속으로 통과하는지로 합니다. 원문 증거 없이 연결 완료로 보고하지 않습니다.

## 실제 프로그램 검증 보고 규칙

- 사용자가 버튼·화면·워크플로우의 실행 검증을 요청하면, 자동 테스트·격리 브라우저·HTTP 응답만으로 `검증 완료` 또는 `해결`이라고 보고하지 않습니다.
- 반드시 사용자의 실제 프로그램 화면(웹앱은 사용자 Chrome의 해당 탭)에서 대상 컨트롤을 직접 활성화하고, 기대한 화면 상태 변화·다음 단계 이동·결과 표시를 다시 읽어 확인합니다.
- 이 실제 화면 재확인이 끝나기 전에는 테스트 통과를 `자동 검증 통과`로만 표현하고, 전체 해결 또는 완료로 표현하지 않습니다.
- 실제 Chrome 검증이 기술적으로 막히면, 완료라고 추정하지 말고 막힌 지점과 이미 확인한 범위를 명확히 보고합니다.
